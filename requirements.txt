# If unsure, run:
# pip freeze > requirements.txt
# then
# pip install -r requirements.txt

# Force pip to install precompiled binaries for all dependencies
--only-binary :all:

requests
tabulate
pandas
dagster
dagster-webserver
pendulum==2.1.2
